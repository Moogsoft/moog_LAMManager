extends layout

block content
  h1= title
  p List of Moobots
   ul
    each bot, i in botList
        li
           a(href='/editor?file=moobots/#{bot}') #{bot}

  p List of Configs
   ul
    each conf, i in confList
        li
           a(href='/editor?file=config/#{conf}') #{conf}
